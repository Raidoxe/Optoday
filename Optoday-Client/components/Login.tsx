import styles from '../styles/Home.module.css';

const Login: React.FC<{socket: any}> = ({socket}) => {
    return (
        <div className={}>
            
        </div>
    )
}

export default Login;